rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для колекції users
    match /users/{userId} {
      // Дозволити читання та запис тільки власних даних
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
